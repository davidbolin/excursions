import(Matrix)

# Export only the main functions
export(excursions)
export(excursions.inla)
export(gaussint)
export(excursions.variances)
export(simconf)
export(continuous)
export(contourmap)
export(contourmap.inla)

useDynLib(excursions2, .registration=TRUE)
